{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://automaniaai.com.br/fabio/schemas/mod-05/charge-update.schema.json",
  "title": "Module 05 Charge Update Contract",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "request": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "request_id": { "type": "string", "format": "uuid" },
        "tenant_id": { "type": "string", "minLength": 1 },
        "source_module": {
          "type": "string",
          "enum": ["mod-01-owner-concierge", "mod-05-faturamento-cobranca"]
        },
        "correlation_id": { "type": "string", "format": "uuid" },
        "changes": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": { "type": "number", "exclusiveMinimum": 0 },
            "currency": { "type": "string", "pattern": "^[A-Z]{3}$" },
            "due_date": { "type": "string", "format": "date" },
            "status": {
              "type": "string",
              "enum": ["draft", "open", "collection_requested", "paid", "canceled", "failed"]
            },
            "metadata": { "type": "object" }
          },
          "minProperties": 1
        }
      },
      "required": ["request_id", "tenant_id", "source_module", "changes"]
    }
  },
  "required": ["request"]
}
