# {{saas_name}} Starter

Generated at: {{generated_at}}

Default tenant: `{{tenant_id}}`
Default layout: `{{layout_default}}`
Default palette: `{{palette_default}}`

## Included Baseline
- Module 01 shell: owner console baseline
- Module 02 shell: crm console baseline
- Module map: modules 1..5 institutional structure
- Theme presets ready for tenant customization

## Next Steps
1. Review `config/theme.json`.
2. Adjust module labels in `config/modules.json`.
3. Adapt tenant presets in `apps/*/theme-preset.json`.
4. Execute runbook steps in `RUNBOOK.md`.
